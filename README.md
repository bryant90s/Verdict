
  # Verdictt

  This is a code bundle for Verdictt. The original project is available at https://www.figma.com/design/nn0ejsl3P4dmQbNJOopErF/Verdictt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  